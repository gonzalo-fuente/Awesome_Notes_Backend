// Define a number for each Role in order to have Role autohrization
// I set all to the same to disable this feature

const ROLES_LIST = {
  "Admin": 2001,
  "Editor": 2001,
  "User": 2001,
};

module.exports = ROLES_LIST;
